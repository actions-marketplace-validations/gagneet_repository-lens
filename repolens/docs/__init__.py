"""Code-reference docs and the docstring-coverage ratchet.

`repolens docs coverage` counts which PUBLIC symbols carry a docstring, per file, and
gates the count as a ratchet; `repolens docs build` renders code-reference HTML with pdoc
(Python) and TypeDoc (TypeScript). Configured by `[docs]` in repolens.toml.
"""
