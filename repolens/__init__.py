"""repolens — traceability and code-understanding tooling for one repository.

Every repository-specific fact (what to scan, what the layers are called, where the
generated artefacts live) is read from `repolens.toml` at the repository root; the
package itself ships only generic defaults.
"""

__version__ = "0.3.0"
